{% extends "@Shop/base.html.twig" %}
{% block title %}Manage{% endblock %}
{% block head %}
    {{ parent() }}


{% endblock %}
{% block content %}
    <div class="col-lg-3 col-md-3 col-sm-4 col-xs-12 col-lg-push-1 col-md-push-1 col-sm-push-2">


        <div class="panel panel-primary">
            <div class="panel-heading"><span>Manage</span></div>
            <div class="panel-body">
                <ul>
                    {% if is_granted('ROLE_EDIT') or is_granted('ROLE_ADMIN') %}
                        <li><a href="{{ path('category_list') }}">Categories</a></li>
                    {% endif %}
                    {% if  is_granted('ROLE_EDIT') or is_granted('ROLE_ADMIN')%}
                        <li><a href="{{ path('product_list') }}">Products</a></li>
                    {% endif %}
                    {% if is_granted('ROLE_EDIT') or is_granted('ROLE_ADMIN')%}
                        <li><a href="{{ path('promotion_list_c') }}">Promptions by Category</a></li>
                    {% endif %}
                    {% if is_granted('ROLE_EDIT') or is_granted('ROLE_ADMIN')%}
                        <li><a href="{{ path('promotion_list_p') }}">Promptions by Products</a></li>
                    {% endif %}
                    {% if is_granted('ROLE_EDIT') or is_granted('ROLE_ADMIN')%}
                        <li><a href="{{ path('promotion_list_u') }}">Promptions by User</a></li>
                    {% endif %}

                </ul>
            </div>
        </div>

        {#<div class="panel panel-primary">#}
            {#<div class="panel-heading"><span></span></div>#}
            {#<div class="panel-body">#}
                {#<ul>#}
                    {#<li><a href=""></a></li>#}

                {#</ul>#}
            {#</div>#}
        {#</div>#}

    </div>
{% endblock %}